package org.example.application.custom.[(${packageName})];

import org.example.application.common.base.BaseDAO;
import org.springframework.stereotype.Component;

import java.util.List;


@Component
public class [(${moduleName})]DAO extends BaseDAO {
    private final [(${moduleName})]Mapper mapping;

    public [(${moduleName})]DAO(ClassRoomMapper mapping) {
        this.mapping = mapping;
    }

    public List<[(${moduleName})]> queryList( ) {
        return mapping.queryList();
    }
}
